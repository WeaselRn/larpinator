export function Footer() {
  return (
    <footer className="mt-16 border-t border-edge/70 py-8">
      <div className="mx-auto flex max-w-6xl flex-col items-center gap-3 px-4 text-center sm:flex-row sm:justify-between sm:text-left">
        <p className="font-mono text-xs text-muted">
          LARPINATOR — the internet&apos;s delulu detector. Built for laughs, not for HR.
        </p>
        <p className="font-mono text-xs text-muted">
          Higher score = more LARP. That&apos;s the whole aesthetic. 🧢
        </p>
      </div>
    </footer>
  );
}
